# SmokeSpot Release Signing Configuration
# ===========================================
#
# 이 파일을 keystore.properties로 복사하고 실제 값을 입력하세요.
# keystore.properties 파일은 .gitignore에 포함되어 있습니다.
#
# 릴리즈 키스토어 생성 방법:
# keytool -genkey -v -keystore smokespot-release.keystore -alias smokespot -keyalg RSA -keysize 2048 -validity 10000
#
# Play Store App Signing 사용 권장:
# https://developer.android.com/studio/publish/app-signing
#

storeFile=smokespot-release.keystore
storePassword=your_keystore_password
keyAlias=smokespot
keyPassword=your_key_password
